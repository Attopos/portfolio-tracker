function SignInPreviewPage() {
  return (
    <section className="page-panel">
      <header className="page-hero">
        <p className="page-eyebrow">Auth Preview</p>
        <h1>Sign-in flow has not moved yet.</h1>
        <p className="page-copy">
          This placeholder keeps navigation coherent while we migrate the shared auth UI
          and Google sign-in flow into React.
        </p>
      </header>
    </section>
  );
}

export default SignInPreviewPage;
